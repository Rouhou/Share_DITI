class TestAssurance{

    // static calculerPrimeMoyenne(assurances) {
    //     if (!assurances || assurances.length === 0) {
    //         throw new Error("Liste d'assurances vide");
    //     }

    //     let somme = 0;
    //     let count = 0;

    //     for (const a of assurances) {
    //         if (a) {
    //             somme += a.calculerPrime();
    //             count++;
    //         }
    //     }
    //     return count === 0 ? 0 : somme / count;
    // }
}